/**
 * ------------------------------------------------------------------
 * Qwen Types
 * ------------------------------------------------------------------
 * Type definitions cho Qwen AI API.
 *
 * Main exports:
 * - QwenCredential : Credential structure (chỉ accessToken)
 * ------------------------------------------------------------------
 */

// ─── Types ──────────────────────────────────────────────────────────────

export interface QwenCredential {
  accessToken: string;
}
